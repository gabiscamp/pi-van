import 'package:flutter/material.dart';

class CreateSalaPage extends StatelessWidget {
  const CreateSalaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Criar Sala'),
      ),
    );
  }
}
