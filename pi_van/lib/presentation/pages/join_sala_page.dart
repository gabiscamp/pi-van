import 'package:flutter/material.dart';

class JoinSalaPage extends StatelessWidget {
  const JoinSalaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Entrar na Sala'),
      ),
    );
  }
}
