import 'package:flutter/material.dart';

class HomeStudentPage extends StatelessWidget {
  const HomeStudentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Estudante'),
      ),
    );
  }
}
